apiVersion: apps/v1
kind: Deployment
metadata:
  name: donut-service
  labels:
    app: donut-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: donut-service
  template:
    metadata:
      labels:
        app: donut-service
    spec:
      containers:
      - name: donut-service
        image: {{ .Values.image }}
        ports:
        - containerPort: 8441
